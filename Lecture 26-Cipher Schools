#include <iostream>
using namespace std;
int main() {
    char a[5];
    for(int i=0;i<=4;i++){
        cin>>a[i];
    
    }
    for(int i=0;i<=4;i++){
    
        cout<<a[i];
    }
    return 0;
}

#include <iostream>
using namespace std;
int main() {
    char a[5]; 
    int i=0;
    while(1){
        cin>>a[i];
        if(a[i]==#){break;}
        i++;
    }
    while(a[i]!="#"){
    
        cout<<a[i];
        
        
    }
    return 0;
}

#include <iostream>
using namespace std;
int main() {
    string name;
    cin>>name;
    cout<<name;
    return 0;
}
